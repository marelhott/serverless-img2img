{
  "name": "serverless-img2img",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "proxy"
  ],
  "scripts": {
    "dev": "npm run dev --workspace proxy & npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "start:proxy": "npm run start --workspace proxy"
  }
}

