{
  "name": "sdxl-img2img-pod",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "build:frontend": "npm run build --workspace frontend",
    "start:local-demo": "IMG2IMG_DEMO_MODE=true python3 -m uvicorn app_runtime.server:app --host 0.0.0.0 --port 8000",
    "start:local": "python3 -m uvicorn app_runtime.server:app --host 0.0.0.0 --port 8000"
  }
}
